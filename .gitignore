# config.js intentionally remains tracked. It contains only the public Supabase
# URL and publishable browser key; never add service_role, database, or Shopee secrets.

# Local editor and OS files
.DS_Store
Thumbs.db

# Supabase CLI local metadata
supabase/.temp/

